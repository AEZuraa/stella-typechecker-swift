language core;

fn is_zero(n : Nat) -> Bool {
    return Nat::iszero(n)
}

fn conditional(n : Nat) -> Nat {
    return if is_zero(n) then succ(0) else n
}

fn main(n : Nat) -> Nat {
    return conditional(n)
}
