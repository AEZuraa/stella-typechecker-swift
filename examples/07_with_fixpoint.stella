language core;
extend with #fixpoint-combinator;

fn identity_fix() -> fn(Nat) -> Nat {
    return fix(fn(f : fn(Nat) -> Nat) {
        return fn(n : Nat) {
            return n
        }
    })
}

fn main(n : Nat) -> Nat {
    return identity_fix()(n)
}
